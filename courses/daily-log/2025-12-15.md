# Daily Log — 2025-12-15 (CDI) — America/La_Paz

## Avances del día (Python)

### 1) DataCamp — Chapter: Dictionaries & Pandas
**Qué vi / practiqué**
- Diccionarios en Python: estructura `key: value`, acceso por clave, actualización y buenas prácticas.
- Operaciones típicas: agregar/editar valores, leer claves/valores, recorrer elementos.
- Puente hacia Pandas: usar estructuras tipo diccionario como base para tabular datos.
- Conceptos clave en Pandas relacionados al capítulo:
  - DataFrame como tabla
  - selección de columnas (y por qué importa pensar en “labels”)

**Lo que me llevo**
- Los diccionarios son la base natural para estructurar datos antes de llevarlos a un DataFrame.
- Si me confundo en Pandas, casi siempre es por no tener claro si estoy trabajando con **labels** (nombres) o con **posiciones**.

---

### 2) Coursera — Python for Data Science — Módulo 3
**Qué vi / practiqué (según lo trabajado hoy en CDI)**
- Programación orientada a objetos (OOP) en Python:
  - `class`, `__init__`, atributos y métodos
  - diferencia entre **atributo de clase** vs **atributo de instancia**
  - uso de `self` y cómo se “guarda” estado dentro del objeto
- Creación de objetos y uso de métodos para modificar/mostrar información (ejemplo: clase `Vehicle`).
- Cómo incluir un “nombre”/identificador del objeto para mostrarlo al imprimir propiedades:
  - enfoque recomendado: agregar un atributo `name` al `__init__` (o al crear el objeto) y usarlo en `display_properties()`.

**Lo que me llevo**
- Entendí mejor el flujo: `clase → constructor → instancias → métodos`.
- Punto crítico: cuándo un valor debe ser de clase (compartido) vs de instancia (propio de cada objeto).

---

## Evidencia (para portafolio)
- Registro de progreso (este log).
- Próximo artefacto recomendado: un mini-ejemplo OOP limpio en un archivo `.py` con:
  - clase, constructor con `name`, método `display_properties()`
  - 2 instancias y salida clara en consola

---

## Próximos pasos (siguiente sesión)
- Consolidar OOP: practicar 2–3 ejercicios cortos (clases con estado + métodos).
- En Pandas: practicar un ejemplo simple (diccionario → DataFrame → selección de columnas/filas).

## Checklist
- [x] DataCamp: Dictionaries & Pandas (capítulo)
- [x] Coursera: Python for Data Science (módulo 3)
- [x] Log listo para GitHub
